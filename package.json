{
	"name": "cangra",
	"version": "1.0.0",
	"private": true,
	"main": "index.js",
	"dependencies": {
		"jquery": "^3.6.0",
		"jquery-ui": "^1.13.2"
	},
	"scripts": {
		"dev": "rm -rf ./development && rm -rf ./.cache && sass --watch src/scss & parcel src/index.html --dist-dir development -p 3000",
		"build": "parcel build src/index.html --no-minify --dist-dir dist --public-url ./"
	},
	"devDependencies": {
		"@babel/core": "^7.18.6",
		"nodemon": "^2.0.19",
		"parcel": "^2.6.2",
		"sass": "^1.53.0"
	}
}
